import { DirectivaJona1Directive } from './directiva-jona1.directive';

describe('DirectivaJona1Directive', () => {
  it('should create an instance', () => {
    const directive = new DirectivaJona1Directive();
    expect(directive).toBeTruthy();
  });
});
